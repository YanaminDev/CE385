const math = 80
const science = 62
const english = 90

const total_score = math + science + english
console.log(`คะเเนนรวม : ${total_score}`)
console.log(`คำนวณค่าเฉลี่ย : ${total_score / 3}`)
console.log(`คำนวณค่าเฉลี่ย > 80 : ${(total_score / 3) > 80 ? true : false}`)
